step 1 

mkdir ai-terminal-tool
cd ai-terminal-tool
nano ai.py

step 2

paste inside nano 

#!/usr/bin/env python3
import sys, subprocess

def ask(question, model="llama3.2"):
    result = subprocess.run(
        ["ollama", "run", model, question],
        capture_output=True, text=True
    )
    print(result.stdout.strip())

if __name__ == "__main__":
    if len(sys.argv) < 2:
        print('Usage: ai "your question"')
        sys.exit(1)
    ask(" ".join(sys.argv[1:]))



step 3

nano setup.sh


step 4

paste inside nano

#!/bin/bash
curl -fsSL https://ollama.com/install.sh | sh
ollama pull llama3.2
chmod +x ai.py
sudo cp ai.py /usr/local/bin/ai
echo "Setup done. Try: ai \"hello\""



step 5

git init
git add ai.py setup.sh
git commit -m "AI terminal tool using Ollama"
git branch -M main
git remote add origin https://github.com/lingesh7-lx/ai-terminal-tool.git
git push -u origin main





step 1

git clone https://github.com/lingesh7-lx/ai-terminal-tool.git
cd ai-terminal-tool

step 2

chmod +x setup.sh
./setup.sh

step 3

ai "what does chmod 755 mean"

